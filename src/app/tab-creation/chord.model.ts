export class Chord {
  public gNote: string;
  public cNote: string;
  public aNote: string;
  public eNote: string;

  constructor(gNote, cNote, aNote, eNote) {
    this.gNote = gNote;
    this.cNote = cNote;
    this.aNote = aNote;
    this.eNote = eNote;
  }
}
